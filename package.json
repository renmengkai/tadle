{
  "name": "tadle",
  "type": "module",
  "bin": "src/main.js",
  "scripts": {
    "start": "node src/main.js",
    "build": "nexe src/main.js --target windows-x64-14.15.3 --output dist/tadle-win.exe && nexe src/main.js --target linux-x64-14.15.3 --output dist/tadle-linux",
    "release": "npm run build && gh release create v$(node -p \"require('./package.json').version\") dist/* --generate-notes"
  },
  "dependencies": {
    "ethers": "^5.7.2",
    "undici": "^7.16.0"
  },
  "devDependencies": {
    "nexe": "^5.0.0-beta.4"
  }
}